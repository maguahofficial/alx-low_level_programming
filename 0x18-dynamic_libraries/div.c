#include <stdio.h>

/**
 * div - function divides the two (intergers)
 * @a: the first (interger)
 * @b: the second (interger)
 * Return: the divided sum
 */
int div(int a, int b)
{
        return(a / b);
}
