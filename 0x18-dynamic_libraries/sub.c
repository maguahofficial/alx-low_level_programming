#include <stdio.h>
#include <stdlib.h>

/**
 * sub - function substracts two (intergers)
 * @a: the first (interger)
 * @b: the second (interger)
 * Return: )
 * Return: the finall result
 */
int sub(int a, int b)
{
        return(a - b);
}
