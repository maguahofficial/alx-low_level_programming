#include <stdio.h>
#include <stdlib.h>

/**
 * mod - mod of the two (intergers)
 * @a: the first interger
 * @b: the second interger
 * Return: the finall mod answer
 */
int mod(int a, int b)
{
        return(a % b);
}
