#include <stdio.h>
#include <stdlib.h>

/**
 * add - function adds two (intergers)
 * @a: the first interger
 * @b: the second interger
 * Return: the finall sum 
 */
int add(int a, int b)
{
	return(a + b);
}
