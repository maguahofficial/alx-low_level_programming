#include <stdio.h>
#include <stdlib.h>

/**
 * mul - function multiplies the two (intergers)
 * @a: the first (interger)
 * @b: the second (interger)
 * Return: the multiplied value
 */
int mul(int a, int b)
{
        return(a * b);
}     
